@model WebApplication21.Models.MovieRentals

@{
    ViewBag.Title = "Random";
}

<h2>@Model.movie.Name</h2>

<h5>Rating: @Model.movie.Rating</h5>

<img src="@Model.movie.ImageUrl" />

@{ 
    int i = 0;
    foreach(var client in Model.clients)
            {

            }
}